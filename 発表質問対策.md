# RISC-Vプロセッサ高速化設計 発表・質問対策

## Q1: なぜ5段パイプラインではWB⇒IDのフォワーディングを行うのか

### 5段パイプラインのデータハザード パターン

5段パイプライン（IF→ID→EX→MEM→WB）では、3命令差のハザードが発生します。

```
命令1: add a1, a2, a3    ← a1 に結果を書き込み（WBステージで確定）
                          |
                          | 3命令差
                          ↓
命令4: add a4, a1, a5    ← a1 の値が必要（IDステージで読み出し）
```

#### WBステージで結果確定、IDステージで読み出し

```
Cycle:     1    2    3    4    5    6    7
命令1:     IF   ID   EX   MEM  WB
                        ↓(結果確定)
                              結果値 WB_RD_VAL
                              ↓ ↓ ↓(フォワード)
命令4:                        IF   ID   ← a1を読み出し
                                  (読み出しタイミング)
```

### フォワーディングなしの場合の問題

**もしフォワーディングなしなら：**

```verilog
// IDステージ：レジスタファイルから読み出し
wire [31:0] ID_RF_DATA1, ID_RF_DATA2;
rf rf(
  .RNUM1(ID_RS1), .RDATA1(ID_RF_DATA1),  ← ここで読み出し
  .WNUM(WB_D_RD),  .WDATA(WB_RD_VAL)     ← ここで書き込み（同じサイクル）
);
```

**問題：** Verilog シミュレーション上、同じサイクル内でレジスタへの非ブロッキング代入（`<=`）は、組み合わせ読み出し（`assign`）に**反映されない**。

つまり、命令4がIDステージで`a1`を読み出すとき、命令1の書き込みはまだ反映されていない。

### WB⇒ID フォワーディングの実装

```verilog
// riscv.v 218-221行（5段パイプライン）
assign ID_RS1_FWD = (WB_D_RD != 5'b0 && WB_D_RD == ID_RS1 && !ID_RS1_PC && !ID_RS1_Z)
                    ? WB_RD_VAL : ID_RF_DATA1;
assign ID_RS2_FWD = (WB_D_RD != 5'b0 && WB_D_RD == ID_RS2)
                    ? WB_RD_VAL : ID_RF_DATA2;
```

**ロジック：**
1. `WB_D_RD != 5'b0` → WBステージが有効なレジスタを書き込んでいるか
2. `WB_D_RD == ID_RS1` → そのレジスタがIDステージで読み出そうとしているレジスタと同じか
3. `!ID_RS1_PC && !ID_RS1_Z` → 特殊な即値（PC、0）ではないか

**全て満たせば：** `WB_RD_VAL`（確定済み値）を直接使用  
**そうでなければ：** レジスタファイルの読み出し値を使用

### なぜ3命令差か

```
命令1: add a1, a2, a3     (WB_RD_VALが確定)
命令2: 別の命令           (パイプ内)
命令3: 別の命令           (パイプ内)
命令4: add a4, a1, a5     (IDステージで a1 が必要)
  ↑
  3命令差 = WBステージから数えて3ステージ下流
```

---

## Q2: なぜ Logic LUT の使用率が 2段PL(2,460) < 逐次(2,529) < 5段PL(2,767) なのか

### 驚くべき結果：逐次 > 2段PL

一見、**逐次実行（シングルサイクル）が最もシンプルなので LUT が最も少ないはず**に思えます。
しかし実測値は逆：**逐次(2,529) > 2段PL(2,460)**

この理由は **シンセシスツール（Xilinx Vivado）の最適化効果** に関係します。

### 逐次実行の組み合わせ論理パス（深い）

逐次CPUはすべての処理が**1つの長い組み合わせパス**で行われます：

```
┌─────────────────────────────────────────────────────┐
│  Cycle 1: すべてを1クロック内に実行                    │
├─────────────────────────────────────────────────────┤
│
│  IF_OP の値が決まる
│    ↓
│  [デコード case文（92-169行）]
│    ↓ ID_FT, ID_IALU, ID_WB_MUX, ID_DMRE, ID_DMWE...
│    ├─→ レジスタ読み出し信号に使用
│    ├─→ ALU制御信号に使用
│    ├─→ メモリアクセス制御に使用
│    └─→ ライトバック MUX制御に使用
│    ↓
│  [レジスタファイル読み出し]
│    ↓ ID_RF_DATA1, ID_RF_DATA2
│    ↓
│  [複数 MUX による入力選択]
│    ├─ PC vs RS1 値の選択
│    ├─ RS2 vs 即値の選択
│    └─ フォワーディング MUX
│    ↓
│  [ALU 実行]
│    ├─ 加減算
│    ├─ シフト
│    ├─ 論理演算
│    └─ 比較
│    ↓
│  [結果の選択]
│    ├─ ALU結果 or シフト結果 or メモリ読み出し値
│    └─ ライトバック MUX（PC+4 or ALU結果 or メモリ値）
│    ↓
│  WB_RD_VAL 出力
│
└─────────────────────────────────────────────────────┘
       ↑
    すべてが1クロック内に成立する必要
    → クリティカルパス短縮のため追加バッファ/レジスタが必要
```

#### シンセシスの問題点

**逐次の場合：**

```verilog
// デコード case文の出力
always @(*) begin
  case(IF_OP)
    `OP_LOAD: begin
      ID_FT = `FT_I;
      ID_WB_MUX = 2'b01;          // ← これがメモリ読み出し制御へ
      ID_DMRE = 2'b11;            // ← これが同時にALU入力へ
    end
  endcase
end

// 同じクロックで：
// ・メモリ読み出しデータが来る
// ・ALU結果も来ている
// ・この両方を選択するライトバック MUXに入力

// → 「ID_WB_MUX が変わる → MEM結果が選ばれる → レジスタに書き込み」
//   という パス全体が1クロック内に成立する必要がある
```

**結果：**
- 深いロジックツリー（15層近くになる可能性）
- シンセシスツールがLUT詰め込み時に効率が落ちる
- **冗長な中間信号が増える**
- **追加のバッファLUTが必要になる**

### 2段パイプラインの構造（浅い）

2段PL（FD + EMW）では、**パイプラインレジスタで論理を分割**：

```
┌─ Stage 1: FD ─────┐  │パイプラインレジスタ│  ┌─ Stage 2: EMW ────┐
│                   │  │                  │  │                    │
│  [デコード case]  │→ │ IF_ID_FT         │→ │ [複数 MUX]         │
│      ↓            │  │ IF_ID_IALU       │  │ [ALU/MEM]          │
│ [Reg読出]         │  │ IF_ID_WB_MUX ... │  │ [ライトバック MUX]│
│      ↓            │  │                  │  │       ↓            │
│  出力確定        │→ │                  │→ │  WB_RD_VAL 出力    │
│                   │  │                  │  │                    │
└───────────────────┘  └──────────────────┘  └────────────────────┘
  20ns以内に完結        （14nsで十分）         12ns以内に完結
   ↑                                          ↑
 深さの制約あり                             制約が緩い
```

**利点：**

1. **Stage 1 と Stage 2 が独立している**
   - Stage 1: デコード + 登録だけ → シンプル
   - Stage 2: MUX + ALU + メモリアクセス → シンプル
   - → 各ステージをシンセシスツールが**個別に最適化**

2. **LUT パッキング効率が高い**
   - Xilinx Artix-7: 6入力 LUT
   - 短いパス：LUT1層で実装可能
   - 逐次の深いパス：複数LUT層が必要
   - → **段数が少ない方が LUT効率良い**

3. **中間結果が「登録」される**
   - `IF_ID_FT` などのレジスタに一度保存
   - Stage 2 でこれを再利用
   - → 同じ計算を2回しない（認識できる）
   - → LUT削減

4. **論理深度の削減**
   ```
   逐次の最悪ケース:
   IF_OP の bit2 → case判定 → ID_FT決定 → MUX選択 → ALU_OP決定 → ALU動作 → WB MUX
                   (層1)      (層2)      (層3)     (層4)       (層5)    (層6+)
                   → 合計15層程度、LUT多用

   2段PLの最悪ケース:
   Stage1: IF_OP → case → ID_FT → レジ保存
           (層1)  (層2) (層3)   (フリップフロップ)
   Stage2: IF_ID_FT → MUX → ALU_OP → ALU
           (層1)      (層2) (層3)    (層4+)
           → 各ステージ5層程度、効率良い
   ```

### 5段パイプラインが最も多い理由

5段PLは2段PLの上に**フォワーディング論理が3経路追加**：

```verilog
// riscv.v 260-271行
wire [1:0] FWD_A_SEL;
assign FWD_A_SEL =
  (EX_MEM_D_RD != 5'b0 && EX_MEM_D_RD == ID_EX_RS1_NUM && EX_MEM_WB_MUX != 2'b01 && !EX_MEM_IS_MUL) ? 2'b01 :
  (MEM_WB_D_RD != 5'b0 && MEM_WB_D_RD == ID_EX_RS1_NUM)                                              ? 2'b10 :
  2'b00;
```

**複雑な条件式 → 複数 LUT 層必要**

結果：

| 実装 | Logic LUT | 特徴 |
|------|-----------|------|
| 2段PL | 2,460 | ✓ 最適化できた短いパス |
| 逐次 | 2,529 | × 深すぎる1つのパス |
| 5段PL | 2,767 | △ フォワーディング分増加 |

### 結論

```
論理深度と LUT の関係：

逐次（深い15層） → 効率悪い → LUT多い(2,529)
                              ↑
2段PL（浅い5層×2） → 効率良い → LUT少ない(2,460) ← 最適
                              ↑
5段PL（浅い5層×3+フォワード） → 複雑 → LUT増加(2,767)

結論：パイプラインレジスタで論理を分割すると、
      シンセシスツールが各部を独立最適化でき、
      反直感的に全体の LUT が **減少することがある** 。
```

---

## Q3: 分岐判定を ID ステージに移動することで、ペナルティを 2サイクル → 1サイクル に短縮する とは

### 現在の 5段PL の分岐処理（EXステージで確定）

```
Cycle:     1    2    3    4    5    6    7
命令1(BEQ):  IF   ID   EX
                      ↓ 分岐確定
後続命令:         IF   ID   ← 既に フェッチ済み（不正命令）
                           ↓ FLUSH !
分岐先:                     IF   ID   EX
                                 ↑ ここにようやく来る

              フラッシュペナルティ = 2サイクル（IF・IDの2段分）
```

#### なぜ 2サイクル フラッシュになるのか

```
Cycle 1: BEQ命令 IF
Cycle 2: BEQ命令 ID
Cycle 3: BEQ命令 EX ← 分岐確定（"not taken"と予測して後続を fetch してた）
         後続命令1 IF  ← 実は不要！
         後続命令2 の準備中
Cycle 4: BEQ命令 MEM
         後続命令1 ID  ← NOP化（フラッシュ）
         後続命令2 IF   ← NOP化（フラッシュ）
         
Cycle 5: BEQ命令 WB
         
Cycle 6: 分岐先命令 IF  ← ここでようやく正しい命令に

合計：IFとIDの2命令がフラッシュされた → 2サイクルペナルティ
```

### 改良案：ID ステージで分岐を確定させる

分岐判定に必要な操作は**非常に簡単**：

```verilog
// BEQ: rs1 == rs2 のとき成立
if (rs1 == rs2) branch_taken = 1;

// BNE: rs1 != rs2 のとき成立  
if (rs1 != rs2) branch_taken = 1;

// BLT: rs1 < rs2（符号付き）のとき成立
if ($signed(rs1) < $signed(rs2)) branch_taken = 1;
```

これらは **単純な比較器＋ゼロチェッカー** だけで実装でき、ALU不要。

#### ID ステージに追加する回路

```
┌─ ID Stage ────────────────────────────────┐
│                                           │
│ IF_ID_RS1_DATA ──┐                       │
│ IF_ID_RS2_DATA ──┼─ [比較器]             │
│ IF_ID_F3 ────────┤ [ゼロチェッカー]      │
│                 └─ BRANCH_DECISION_EARLY│
│                                           │
│  ↓                                        │
│  この時点で分岐成立がわかる               │
│  → PC = BRANCH_ADDR (ID + imm)           │
│                                           │
└─────────────────────────────────────────┘
```

### 改良後：1サイクル フラッシュ

```
Cycle:     1    2    3    4    5    6
命令1(BEQ):  IF   ID ← 分岐確定！
             
後続命令:         IF   ← NOP化（フラッシュ）
                    ↓ FLUSH !
分岐先:              ID   EX   MEM   WB

              フラッシュペナルティ = 1サイクル（IF の 1段分のみ）
```

#### なぜ 1サイクル で済むのか

```
Cycle 1: BEQ命令 IF
Cycle 2: BEQ命令 ID ← 分岐確定（比較器で即座に判定）
         後続命令 IF  ← (同じサイクルで)この時点で不正とわかる
Cycle 3: BEQ命令 EX 
         後続命令 の代わりに 分岐先命令 IF ← さっき PC を更新したから
Cycle 4: BEQ命令 MEM
         分岐先命令 ID

合計：IFの1命令がフラッシュされただけ → 1サイクルペナルティ
```

### クイックソートへの効果

**現状（EXで確定）：**
- 分岐フラッシュ: 17,983回 × 2サイクル = 35,966サイクル
- 実行サイクル: 165,507
- CPI: 1.3934

**改良後（IDで確定）：**
- 分岐フラッシュ: 17,983回 × 1サイクル = 17,983サイクル
- 実行サイクル: 165,507 - 17,983 = 147,524
- CPI: 147,524 / 118,777 = **1.242**
- 実行時間: 118,777 × 1.242 × 12ns ≈ **1.770 ms**

**比較：**
```
現在の5段PL:  1.986 ms  (逐次比: 1.20×)
改良後:      1.770 ms  (逐次比: 1.34×) ✓ 2段PLより速い！
2段PL:       1.914 ms  (逐次比: 1.24×)
```

### 実装上の注意点

1. **比較器の遅延**
   - 32ビット等値判定: 低遅延（簡単）
   - 符号付き大小比較: やや遅延あり
   - ただしALUほどではない

2. **メモリアクセス命令は分岐ではない**
   - LOAD/STORE では不要
   - 分岐命令（BEQ/BNE/BLT/JAL など）のみ適用

3. **削減の制限要因**
   - ID段で使えるのは「ソースレジスタの値」
   - WB→IDフォワーディング が有効に機能していれば、ID段でもレジスタ値が確定
   - → クイックソートのような頻繁な分岐でも対応可能

---

## 補足：3つの効果を組み合わせた場合

1. **分岐予測** (Branch Prediction)
   - 精度 80%: CPI ≈ 1.061
   - 実行時間 ≈ 1.512 ms

2. **ID段での分岐確定** (Early Branch Resolution)
   - CPI ≈ 1.242
   - 実行時間 ≈ 1.770 ms

3. **分岐予測 + ID段での確定**
   - CPI ≈ 1.060 (ほぼ予測と同等)
   - 実行時間 ≈ 1.512 ms

4. **LOAD-USE スケジューリング** (コンパイラ工夫)
   - 追加効果: ～5% の削減

---

## まとめ表

| 質問 | 要点 |
|------|------|
| Q1: WB→ID フォワーディングの必要性 | **Verilog シミュレーション上、同一クロックの NB代入が組み合わせ読み出しに反映されないため、3命令差ハザード時に明示的フォワーディングが必須** |
| Q2: Logic LUT の逆転現象 | **パイプラインレジスタで論理を分割すると、シンセシスが各ステージを独立最適化でき、反直感的に LUT 削減が起きる。逐次は深すぎるパスのため効率悪い** |
| Q3: 分岐判定の ID 段移動 | **比較器は ALU より簡単・低遅延。ID で分岐確定 → フラッシュ 1命令に削減 → ペナルティ 50%削減** |
